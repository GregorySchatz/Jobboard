<header>
    <nav class="navbar navbar-expand-lg bcgColorBlue">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">JoBoard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href={{path ('home.index')}}>Home</a>
                    </li>
                    {% if is_granted('ROLE_USER') == false%}
                    <li class="nav-item">
                        <a class="nav-link" href={{path ('security.login')}}>Login</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_COMPANY') %}
                    <li class="nav-item">
                        <a class="nav-link" href={{path ('app_form_advertisement')}}>Create advertisement</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_COMPANY') %}
                    <li class="nav-item">
                        <a class="nav-link" href={{path ('app_show_job_application')}}>Show job application</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a style="position:absolute;right:10px;"class="nav-link" href={{path('security.logout')}}>Logout</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_COMPANY') %}
                    <li class="nav-item">
                        <a class="nav-link" href={{path ('app_show_advertisement')}}>My advertisements</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href={{path ('security.editAccount')}}>Profile</a>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="./admin">Dashboard Admin</a>
                    </li>
                    
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>